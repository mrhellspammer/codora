package com.codora.backend.model;

// Enum to represent the type of content blocks inside a module
public enum ContentType {
    HEADING,
    SUBHEADING,
    TEXT,
    CODE,
    IMAGE  // ← we added this too during content image support
}
